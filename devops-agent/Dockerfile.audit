FROM python:3.11-slim

WORKDIR /app

# Simple dependencies
RUN pip install --no-cache-dir fastapi uvicorn httpx

# Install staged code
COPY . /app

WORKDIR /app/vendor/talos-audit-service

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8081"]
